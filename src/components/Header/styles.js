import { css } from '@emotion/react';

export const title = css`
  display: block;
  margin: 8px 0;
  font-size: 24px;
  position: relative;
`;

export const header = css`
  font-family: var(--font-vcr);
`;
